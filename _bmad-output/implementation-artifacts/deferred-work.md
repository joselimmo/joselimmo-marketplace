# Deferred Work

This file collects items that were surfaced in code reviews but intentionally deferred to a later story or future work. Each entry preserves the original reviewer's reasoning so a future contributor can decide whether the deferral is still valid.

## Deferred from: code review of story 1-1-project-bootstrap-monorepo-scaffold-dual-license-layout (2026-04-26)

- **`.gitattributes` rule targets a future path** — `packages/core/src/diagnostics/codes.generated.ts merge=ours linguist-generated=true` is dangling until Story 2.2 creates that file. If Story 2.2 lands the generated file at a different subpath, this rule silently no-ops. Owner: Story 2.2 (verify path alignment). [`caspian/.gitattributes:2`]
- **`noDefaultExport` future conflict with config files** — Lint rule with no `overrides` block will reject `vitest.config.ts`, `rollup.config.ts`, Vite/Next.js conventions, etc. when those land. Owner: Stories 2.1+ (add `overrides` then). [`caspian/biome.json:32`]
- **Lockfile reproducibility verified single-platform** — `pnpm install --frozen-lockfile` exits 0 on Windows/x64 only; multi-OS guarantee depends on a CI matrix that does not exist yet. Owner: Story 2.8 (release.yml) + future `ci.yml`. [`caspian/pnpm-lock.yaml`]
- **`release` script incomplete** — `package.json` `release: "changeset publish"` lacks the canonical preceding `changeset version` step. Bootstrap placeholder; Story 2.8 wires the full release flow. Owner: Story 2.8. [`caspian/package.json:17`]
- **CLI name availability** — Root package is `caspian-monorepo`; published CLI will be unscoped `caspian`. Verify npm name availability before Story 2.1 ships `packages/cli`. [`caspian/package.json:2`]
- **Node 20 LTS sunset** — `.nvmrc=20.10` while Node 22 LTS has taken over (Node 20 LTS ends ~April 2026). Architecture step-04 F1 calls for a Node 20 + 22 CI matrix that does not yet exist. Owner: future infra story. [`caspian/.nvmrc`]
- **Changesets schema URL drift** — `caspian/.changeset/config.json` pins `$schema` to `@changesets/config@3.1.4` while `@changesets/cli` floats `^2.31.0`. Auto-generated by `changeset init`; revisit on next changesets upgrade. [`caspian/.changeset/config.json:2`]
- **No canary fixtures for biome rules** — `useFilenamingConvention` and `noDefaultExport` are not asserted by any test; a future biome major bump that silently relocates the rule could regress detection without noise. Owner: Story 2.1 (first source files). [`caspian/biome.json`]
- **README CTA links return 404 on GitHub** — The 4-CTA hub (`spec/`, `packages/cli/`, `plugins/casper-core/`, `spec/CONTRIBUTING.md`) is mandated by Task 2 spec but every link 404s today. Resolves as each target story merges (1.2, 2.1+, 3.1+, 5.1). [`caspian/README.md:7-10`]
- **`engines.node` upper-bound** — `>=20.10` allows EOL Node 21 and non-LTS Node 23. If Decision 2 of the review chose to defer rather than tighten now, this remains an open infra item. [`caspian/package.json:10`]

## Deferred from: code review of story 1-3-canonical-core-vocabulary-docs (2026-04-26)

- **core.md missing explicit anchors for `#schema-evolution` and `#extension-mechanisms`** — `adr.md` links to `../core.md#schema-evolution`; `plan.md` and `story.md` link to `../core.md#extension-mechanisms`. Both headings exist but carry no explicit `{#...}` anchor, so the links depend on renderer-generated slugs. The links resolve correctly on GitHub today but are fragile: a future heading rename silently breaks them. core.md is sealed by Story 1.2. Fix: either add explicit anchors to `core.md` in a cleanup ticket, or remove the anchor fragments from the three vocabulary docs and link to `../core.md` without a fragment. [`caspian/spec/core.md` lines 188, 232; `caspian/spec/vocabulary/adr.md`, `plan.md`, `story.md`]

## Deferred from: code review of 1-4-envelope-json-schema-draft-2020-12 (2026-04-27)

- **`count` field name implies exact count; description says "minimum"** — Field is named `count` in `core.md`; runtime semantics are "at-least-N". Renaming (e.g., to `min_count`) is a breaking spec change and out of scope for Story 1.4. Revisit at spec v0.2+ RFC. [`caspian/schemas/v1/envelope.schema.json` — `$defs.RequiresEntry.count`]
- **`schema_version` accepts empty string (no `minLength`)** — Schema-level rejection of unrecognized versions would convert CASPIAN-W003 (warn-on-unknown) into an error, breaking the graceful-degradation contract (NFR16). Empty/unrecognized values handled by the warning system in Story 2.4. No change needed unless the warn-on-unknown policy changes. [`caspian/schemas/v1/envelope.schema.json` — `properties.schema_version`]
- **`tags` array lacks `minItems: 1`, `uniqueItems: true`, `items.minLength: 1`** — Empty tags array is semantically equivalent to omitting the field; duplicate/empty-string tags are noise. Not addressed by any Story 1.4 AC. Could be tightened in a future schema hardening story (post-v1.0 or at Story 1.6 fixture authoring if needed). [`caspian/schemas/v1/envelope.schema.json` — `$defs.RequiresEntry.tags`]
- **`requires: []` (empty array) passes validation** — `minItems: 1` not required by any AC. An empty requires array is semantically equivalent to omitting the field; open design question whether it should be forbidden at schema level. [`caspian/schemas/v1/envelope.schema.json` — `properties.requires`]
- **`caspian/LICENSE` leading blank line (Story 1.1 artifact)** — Both `caspian/LICENSE` and `caspian/schemas/LICENSE` open with a blank line before the Apache License header. Byte-identical per `diff`. Non-standard vs. canonical Apache 2.0 text; may cause SPDX tooling mismatch. Cleanup belongs in a housekeeping commit or Story 1.1 follow-up. [`caspian/LICENSE`, `caspian/schemas/LICENSE`]
- **`schema_version: 0.1` YAML float-cast trap** — Unquoted `schema_version: 0.1` in YAML frontmatter is parsed as float `0.1` by YAML 1.1 auto-cast, causing a type error against the `"type": "string"` schema constraint. Fix is a documentation/prose note in the spec (`core.md`) reminding authors to quote the value. Handled by the YAML pipeline in Story 2.3. [`caspian/spec/core.md` — `schema_version` authoring guidance]
- **No upper bound on `count` integer** — `count` has `minimum: 1` but no `maximum`. Very large integers pass. No practical upper bound required by any AC or architecture constraint; could add `maximum` in a future hardening story if tooling needs it. [`caspian/schemas/v1/envelope.schema.json` — `$defs.RequiresEntry.count`]
- **`Produces.additionalProperties: false` limits future extensibility** — Strict `additionalProperties: false` means adding any new field to `produces` (e.g., `schema_version`, `tags`) requires a schema major bump per NFR22 BACKWARD_TRANSITIVE policy. This is the intentional Design A5 asymmetry (inner shapes locked, outer envelope open). Future spec evolution must introduce a `v2/` directory. Not actionable in Story 1.4. [`caspian/schemas/v1/envelope.schema.json` — `$defs.Produces`]
