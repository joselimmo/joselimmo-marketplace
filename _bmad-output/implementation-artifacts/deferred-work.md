# Deferred Work

This file collects items that were surfaced in code reviews but intentionally deferred to a later story or future work. Each entry preserves the original reviewer's reasoning so a future contributor can decide whether the deferral is still valid.

## Deferred from: code review of story 1-1-project-bootstrap-monorepo-scaffold-dual-license-layout (2026-04-26)

- **`.gitattributes` rule targets a future path** — `packages/core/src/diagnostics/codes.generated.ts merge=ours linguist-generated=true` is dangling until Story 2.2 creates that file. If Story 2.2 lands the generated file at a different subpath, this rule silently no-ops. Owner: Story 2.2 (verify path alignment). [`caspian/.gitattributes:2`]
- **`noDefaultExport` future conflict with config files** — Lint rule with no `overrides` block will reject `vitest.config.ts`, `rollup.config.ts`, Vite/Next.js conventions, etc. when those land. Owner: Stories 2.1+ (add `overrides` then). [`caspian/biome.json:32`]
- **Lockfile reproducibility verified single-platform** — `pnpm install --frozen-lockfile` exits 0 on Windows/x64 only; multi-OS guarantee depends on a CI matrix that does not exist yet. Owner: Story 2.8 (release.yml) + future `ci.yml`. [`caspian/pnpm-lock.yaml`]
- **`release` script incomplete** — `package.json` `release: "changeset publish"` lacks the canonical preceding `changeset version` step. Bootstrap placeholder; Story 2.8 wires the full release flow. Owner: Story 2.8. [`caspian/package.json:17`]
- **CLI name availability** — Root package is `caspian-monorepo`; published CLI will be unscoped `caspian`. Verify npm name availability before Story 2.1 ships `packages/cli`. [`caspian/package.json:2`]
- **Node 20 LTS sunset** — `.nvmrc=20.10` while Node 22 LTS has taken over (Node 20 LTS ends ~April 2026). Architecture step-04 F1 calls for a Node 20 + 22 CI matrix that does not yet exist. Owner: future infra story. [`caspian/.nvmrc`]
- **Changesets schema URL drift** — `caspian/.changeset/config.json` pins `$schema` to `@changesets/config@3.1.4` while `@changesets/cli` floats `^2.31.0`. Auto-generated by `changeset init`; revisit on next changesets upgrade. [`caspian/.changeset/config.json:2`]
- **No canary fixtures for biome rules** — `useFilenamingConvention` and `noDefaultExport` are not asserted by any test; a future biome major bump that silently relocates the rule could regress detection without noise. Owner: Story 2.1 (first source files). [`caspian/biome.json`]
- **README CTA links return 404 on GitHub** — The 4-CTA hub (`spec/`, `packages/cli/`, `plugins/casper-core/`, `spec/CONTRIBUTING.md`) is mandated by Task 2 spec but every link 404s today. Resolves as each target story merges (1.2, 2.1+, 3.1+, 5.1). [`caspian/README.md:7-10`]
- **`engines.node` upper-bound** — `>=20.10` allows EOL Node 21 and non-LTS Node 23. If Decision 2 of the review chose to defer rather than tighten now, this remains an open infra item. [`caspian/package.json:10`]
