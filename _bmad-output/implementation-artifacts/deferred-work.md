# Deferred Work

This file collects items that were surfaced in code reviews but intentionally deferred to a later story or future work. Each entry preserves the original reviewer's reasoning so a future contributor can decide whether the deferral is still valid.

## Deferred from: code review of story 1-1-project-bootstrap-monorepo-scaffold-dual-license-layout (2026-04-26)

- **`.gitattributes` rule targets a future path** — `packages/core/src/diagnostics/codes.generated.ts merge=ours linguist-generated=true` is dangling until Story 2.2 creates that file. If Story 2.2 lands the generated file at a different subpath, this rule silently no-ops. Owner: Story 2.2 (verify path alignment). [`caspian/.gitattributes:2`]
- **`noDefaultExport` future conflict with config files** — Lint rule with no `overrides` block will reject `vitest.config.ts`, `rollup.config.ts`, Vite/Next.js conventions, etc. when those land. Owner: Stories 2.1+ (add `overrides` then). [`caspian/biome.json:32`]
- **Lockfile reproducibility verified single-platform** — `pnpm install --frozen-lockfile` exits 0 on Windows/x64 only; multi-OS guarantee depends on a CI matrix that does not exist yet. Owner: Story 2.8 (release.yml) + future `ci.yml`. [`caspian/pnpm-lock.yaml`]
- **`release` script incomplete** — `package.json` `release: "changeset publish"` lacks the canonical preceding `changeset version` step. Bootstrap placeholder; Story 2.8 wires the full release flow. Owner: Story 2.8. [`caspian/package.json:17`]
- **CLI name availability** — Root package is `caspian-monorepo`; published CLI will be unscoped `caspian`. Verify npm name availability before Story 2.1 ships `packages/cli`. [`caspian/package.json:2`]
- **Node 20 LTS sunset** — `.nvmrc=20.10` while Node 22 LTS has taken over (Node 20 LTS ends ~April 2026). Architecture step-04 F1 calls for a Node 20 + 22 CI matrix that does not yet exist. Owner: future infra story. [`caspian/.nvmrc`]
  - **Resolved 2026-04-27 (Epic 1 retro AI-2):** `caspian/.nvmrc` bumped `20.10` → `22.13`; `caspian/package.json` `engines.node` bumped `>=20.10` → `>=22.13`. Spec-side already done in retro pre-pass (epics.md + architecture.md `engines.node` references + `node:22-alpine` docker gate). Multi-OS CI matrix remains owned by Story 2.8 (release.yml) per the next entry below.
- **Changesets schema URL drift** — `caspian/.changeset/config.json` pins `$schema` to `@changesets/config@3.1.4` while `@changesets/cli` floats `^2.31.0`. Auto-generated by `changeset init`; revisit on next changesets upgrade. [`caspian/.changeset/config.json:2`]
- **No canary fixtures for biome rules** — `useFilenamingConvention` and `noDefaultExport` are not asserted by any test; a future biome major bump that silently relocates the rule could regress detection without noise. Owner: Story 2.1 (first source files). [`caspian/biome.json`]
- **README CTA links return 404 on GitHub** — The 4-CTA hub (`spec/`, `packages/cli/`, `plugins/casper-core/`, `spec/CONTRIBUTING.md`) is mandated by Task 2 spec but every link 404s today. Resolves as each target story merges (1.2, 2.1+, 3.1+, 5.1). [`caspian/README.md:7-10`]
- **`engines.node` upper-bound** — `>=20.10` allows EOL Node 21 and non-LTS Node 23. If Decision 2 of the review chose to defer rather than tighten now, this remains an open infra item. [`caspian/package.json:10`]
  - **Partially resolved 2026-04-27 (Epic 1 retro AI-2):** floor bumped to `>=22.13` (current Node 22 LTS), so the previously-allowed EOL Node 20/21 ranges are now excluded. The asymmetric "no upper bound" concern (non-LTS Node 23 still passes the predicate) is unchanged and remains owned by the future CI matrix story (Story 2.8 + future `ci.yml`).

## Deferred from: code review of story 1-3-canonical-core-vocabulary-docs (2026-04-26)

- **core.md missing explicit anchors for `#schema-evolution` and `#extension-mechanisms`** — `adr.md` links to `../core.md#schema-evolution`; `plan.md` and `story.md` link to `../core.md#extension-mechanisms`. Both headings exist but carry no explicit `{#...}` anchor, so the links depend on renderer-generated slugs. The links resolve correctly on GitHub today but are fragile: a future heading rename silently breaks them. core.md is sealed by Story 1.2. Fix: either add explicit anchors to `core.md` in a cleanup ticket, or remove the anchor fragments from the three vocabulary docs and link to `../core.md` without a fragment. [`caspian/spec/core.md` lines 188, 232; `caspian/spec/vocabulary/adr.md`, `plan.md`, `story.md`]

## Deferred from: code review of 1-4-envelope-json-schema-draft-2020-12 (2026-04-27)

- **`count` field name implies exact count; description says "minimum"** — Field is named `count` in `core.md`; runtime semantics are "at-least-N". Renaming (e.g., to `min_count`) is a breaking spec change and out of scope for Story 1.4. Revisit at spec v0.2+ RFC. [`caspian/schemas/v1/envelope.schema.json` — `$defs.RequiresEntry.count`]
- **`schema_version` accepts empty string (no `minLength`)** — Schema-level rejection of unrecognized versions would convert CASPIAN-W003 (warn-on-unknown) into an error, breaking the graceful-degradation contract (NFR16). Empty/unrecognized values handled by the warning system in Story 2.4. No change needed unless the warn-on-unknown policy changes. [`caspian/schemas/v1/envelope.schema.json` — `properties.schema_version`]
- **`tags` array lacks `minItems: 1`, `uniqueItems: true`, `items.minLength: 1`** — Empty tags array is semantically equivalent to omitting the field; duplicate/empty-string tags are noise. Not addressed by any Story 1.4 AC. Could be tightened in a future schema hardening story (post-v1.0 or at Story 1.6 fixture authoring if needed). [`caspian/schemas/v1/envelope.schema.json` — `$defs.RequiresEntry.tags`]
- **`requires: []` (empty array) passes validation** — `minItems: 1` not required by any AC. An empty requires array is semantically equivalent to omitting the field; open design question whether it should be forbidden at schema level. [`caspian/schemas/v1/envelope.schema.json` — `properties.requires`]
- **`caspian/LICENSE` leading blank line (Story 1.1 artifact)** — Both `caspian/LICENSE` and `caspian/schemas/LICENSE` open with a blank line before the Apache License header. Byte-identical per `diff`. Non-standard vs. canonical Apache 2.0 text; may cause SPDX tooling mismatch. Cleanup belongs in a housekeeping commit or Story 1.1 follow-up. [`caspian/LICENSE`, `caspian/schemas/LICENSE`]
- **`schema_version: 0.1` YAML float-cast trap** — Unquoted `schema_version: 0.1` in YAML frontmatter is parsed as float `0.1` by YAML 1.1 auto-cast, causing a type error against the `"type": "string"` schema constraint. Fix is a documentation/prose note in the spec (`core.md`) reminding authors to quote the value. Handled by the YAML pipeline in Story 2.3. [`caspian/spec/core.md` — `schema_version` authoring guidance]
- **No upper bound on `count` integer** — `count` has `minimum: 1` but no `maximum`. Very large integers pass. No practical upper bound required by any AC or architecture constraint; could add `maximum` in a future hardening story if tooling needs it. [`caspian/schemas/v1/envelope.schema.json` — `$defs.RequiresEntry.count`]
- **`Produces.additionalProperties: false` limits future extensibility** — Strict `additionalProperties: false` means adding any new field to `produces` (e.g., `schema_version`, `tags`) requires a schema major bump per NFR22 BACKWARD_TRANSITIVE policy. This is the intentional Design A5 asymmetry (inner shapes locked, outer envelope open). Future spec evolution must introduce a `v2/` directory. Not actionable in Story 1.4. [`caspian/schemas/v1/envelope.schema.json` — `$defs.Produces`]

## Deferred from: code review of 1-5-diagnostic-registry-registry-schema (2026-04-27)

- **`caspian/spec/core.md` line 82 references `CASPIAN-W004` as "proposed ... to be reserved by Story 1.5's registry"** — the architecture's v1.0 set (lines 277–279) has only W001/W002/W003; Story 1.5 followed the architecture and did NOT reserve W004. `core.md` is sealed (Story 1.2). A future tidy-up story could either (a) remove the W004 reference from `core.md`, or (b) introduce W004 in a v0.2+ RFC for the "type under `core:` whose name is not in the canonical vocabulary" warning use case. [`caspian/spec/core.md` lines 80–84]
- **Schema has no cross-field constraint preventing `code`/`severity` letter mismatch** — `CASPIAN-E001` with `severity: "warning"` passes schema validation; JSON Schema 2020-12 cross-field constraints require `if/then/else` complexity. All 17 current entries are correct by construction. Consider enforcing via CI script or schema v2 `if/then/else`. [`caspian/schemas/v1/diagnostic-registry.schema.json`]
- **`doc` URL numeric suffix is not constrained to match the `code` numeric suffix** — `code: "CASPIAN-E001"` with `doc: "…#caspian-e099"` passes schema validation; JSON Schema cannot enforce numeric equality across fields. Story 2.2/4.2 generators should verify alignment at generation time. [`caspian/schemas/v1/diagnostic-registry.schema.json`]
- **No `uniqueItems: true` on the `diagnostics` array** — duplicate full-object entries pass schema validation; `uniqueItems` compares full objects, not just `code` values (imperfect fix). Consider CI enforcement via `ajv-validate-registry` step (Epic 2) or schema v2. [`caspian/schemas/v1/diagnostic-registry.schema.json`]
- **No kebab-case pattern constraint on the `rule` field** — only `minLength: 1` enforced; all 17 current entries use kebab-case correctly. A future schema v2 could add `pattern: "^[a-z][a-z0-9]*(-[a-z0-9]+)*$"`. [`caspian/schemas/v1/diagnostic-registry.schema.json`]
- **`message` field has no `maxLength` constraint** — current 17 messages are concise (26–69 chars); consider `maxLength: 200` in schema v2 to guard against CLI output truncation. [`caspian/schemas/v1/diagnostic-registry.schema.json`]
- **CHANGELOG.md cross-references future CHANGELOGs that don't exist yet** — `spec/CHANGELOG.md`, `packages/cli/CHANGELOG.md`, `packages/core/CHANGELOG.md` are referenced but absent. Resolves as Epic 2 (Story 2.8) and Story 5.2 ship. [`caspian/diagnostics/CHANGELOG.md`]
- **No machine-readable `version` field in `registry.json`** — CHANGELOG states decoupled semver but registry has no version tag; `additionalProperties: false` at root prevents adding one without a `schemas/v2/` bump. Version tracking is prose-only via CHANGELOG. [`caspian/diagnostics/registry.json`]
- **CASPIAN-E009 message contains literal angle brackets (`<namespace>:<name>`)** — canonical architecture text; `<` and `>` are intentional pattern-syntax notation. Story 4.2 MUST apply standard HTML entity escaping to all registry string fields before rendering. [`caspian/diagnostics/registry.json`]
- **No uniqueness constraint on the `rule` field** — two entries could share the same rule name with different codes; all 17 current rules are unique. Story 2.2 TypeScript generator should detect and reject duplicate rule names. [`caspian/schemas/v1/diagnostic-registry.schema.json`]
- **Append-only governance is process-dependent, not schema-enforced** — no CI step `ajv-validate-registry` yet; already recorded as Forward Dependency in Completion Notes. CI step lands in Epic 2. [`caspian/schemas/v1/diagnostic-registry.schema.json`]

## Deferred from: code review of 1-6-canonical-fixture-set-valid-invalid (2026-04-27)

- **Line-number convention for file-level stage-1/stage-2 rejections** — E002 `"line":1` but first invalid byte (0x91) is at line 3; E004 `"line":1` but oversized content starts at line 3. Whether the convention is "line 1 = file-level anchor" or "first offending line" is undefined in the spec. Story 2.6 authoritative line-number reconciliation (golden-snapshot) will resolve this definitively and update fixture expected values as needed. [`caspian/fixtures/invalid/E002-encoding/non-utf8.expected.json`, `caspian/fixtures/invalid/E004-oversized/over-4kb.expected.json`]
- **E008 `"line":1` anchor for absent-field diagnostics** — When a required field is simply absent, there is no line to point to; `"line":1` is used as a convention but is not documented in AC9's contract. Epic 2 validator implementation should nail down this convention. [`caspian/fixtures/invalid/E008-type-missing/no-type.expected.json`]
- **No fixture for `type: ""` (empty type string)** — CASPIAN-E008 registry message says "missing or empty" but the empty-string case is untested. A future story (2.3 or 2.4 test hardening) should add `E008-type-empty/empty-type.md` + `empty-type.expected.json`.
- **No fixture for `requires: []` (empty array)** — Schema has no `minItems`; empty `requires` passes validation but is semantically vacuous. Behavioral edge case for downstream tooling. Open design question whether this should be rejected.
- **No fixture for scalar non-string `requires` entry** — `requires: [42]` or `requires: [null]` (neither string nor object) is unrepresented. Gap in invalid coverage for E011/E012 boundary conditions.
- **No fixture for zero-byte / whitespace-only file** — A file containing only whitespace has no opening `---` (distinct from E005 which has an opening delimiter). Realistic edge case for the stage-1 byte-level check.

## Deferred from: code review of 1-8-caspian-w004-registry-reservation (2026-04-27)

- **`doc` URL is a dead forward-reference** — `https://caspian.dev/diagnostics#caspian-w004` is committed as a fact but the page does not yet exist; no draft/provisional marker. Pre-existing pattern on all 18 entries — none of the caspian.dev anchor pages exist pre-launch. Resolves when caspian.dev goes live (Epic 4). [`caspian/diagnostics/registry.json` — W004 entry]
- **`expected.json` records only `code`+`line` — no `severity` or `rule`** — The expected manifest is underspecified; a regression that downgrades W004 to `info` or renames its rule slug would not be caught by the fixture test. Pre-existing convention matching W002/W003 expected.json format. Revisit in Epic 2 Story 2.6 (JSON golden snapshots) if richer assertion coverage is desired. [`caspian/fixtures/invalid/W004-non-canonical-core-name/non-canonical-name.expected.json`]
- **`line: 2` in expected manifest is fragile** — A prepended blank line or comment in the fixture `.md` shifts the `type:` field off line 2 without breaking the fixture file itself. Pre-existing concern affecting all line-number assertions in the fixture set. Durable fix: Story 2.6 golden-snapshot reconciliation. [`caspian/fixtures/invalid/W004-non-canonical-core-name/non-canonical-name.expected.json:3`]
- **No valid fixture complementing W004** — A `valid/` fixture demonstrating a recognized `core:` name (e.g., `core:story`) that should produce zero diagnostics is absent; a broken implementation that always (or never) emits W004 cannot be distinguished from this fixture set alone. Out of scope for Story 1.8. Candidate for Epic 2 test hardening.
- **Rule slug naming inconsistency across W001–W004** — W004 uses the negative form `core-namespace-name-not-in-vocabulary` while W003 uses the positive form `schema-version-recognized`; W001/W002 use short problem-descriptor forms. W004 slug is spec-prescribed in Story 1.8 AC2 — the convention inconsistency is registry-wide and pre-dates W004. Candidate for a naming-convention RFC in Epic 5. [`caspian/diagnostics/registry.json`]
- **No runtime schema re-validation CI step after registry additions** — No CI step (`ajv-validate-registry`) currently rejects a malformed new entry; Story 1.8 cross-checks are manual. Epic 2 Story 2.1 lands the validator runtime; CI enforcement follows in Story 2.2/2.7. [`caspian/schemas/v1/diagnostic-registry.schema.json`]
- **Multi-colon `type` values like `core:story:v2` — validator behavior undefined** — Whether namespace extraction takes the first colon-delimited segment only or the full prefix is not specified. No fixture covers this boundary. Epic 2 validator implementation must define and test this. [`caspian/diagnostics/registry.json` — W004 rule; `caspian/fixtures/invalid/W004-non-canonical-core-name/`]
- **Case-variant core names (`core:Story`, `core:STORY`) — vocabulary check case-sensitivity unspecified** — Whether `core:Story` triggers W004 is not specified in the registry or spec. Epic 2 validator must define normalization behavior. [`caspian/diagnostics/registry.json` — W004 rule]
- **W004 scope on `requires`/`produces` nested `type` fields** — Whether W004 applies only to the top-level `type` field or also to nested `type` values in `requires[]`/`produces` is unspecified. Epic 2 Story 2.4 implementation must clarify and test this. [`caspian/diagnostics/registry.json` — W004 rule]
- **YAML whitespace in `type` value (`core: nonexistent`) — normalization unspecified** — A `type` value with whitespace around the namespace colon may parse differently across YAML parsers. The validator must normalize or reject such inputs. Epic 2 validator scope. [`caspian/fixtures/invalid/W004-non-canonical-core-name/non-canonical-name.md`]

## Deferred from: code review of 2-1-caspian-core-skeleton-envelope-schema-integration-loader-ts (2026-04-27)

- **`fs.access()` TOCTOU race in `validateFile`** — `fs.access()` then unrelated validator call creates a check-then-not-use pattern; also succeeds for directories and broken symlinks. Production implementation (Story 2.3+) will replace with full `fs.readFile()`. [`caspian/packages/core/src/index.ts:6`]
- **Cache concurrency double-initialization risk** — Both `loadEnvelopeSchema` and `getEnvelopeValidator` use a null-check guard without a promise-sentinel lock. Concurrent callers can trigger double-initialization; harmless correctness-wise in Node.js single-threaded event loop (both loads produce equivalent results), minor memory waste. [`caspian/packages/core/src/schemas/loader.ts:12-13`, `src/validator.ts:6-7`]
- **`object | null` type for cached schema — structural type guarantees lost** — `JSON.parse(raw) as object` widens to the near-useless `object` type. Story 2.2+ can refine to a proper `SchemaObject` type once ajv types are better established in the project. [`caspian/packages/core/src/schemas/loader.ts:11`]
- **`noRestrictedImports` depth coverage gap (depths 0 and 5+)** — biome 2.4.13 glob fallback covers only `../` through `../../../../` ascent depths. Depths 0 (e.g., `./schemas/...`) and 5+ bypass the rule. Acknowledged in Completion Notes. Story 2.7 conformance suite can consolidate if biome gains regex/glob support. [`caspian/biome.json:36-43`]
- **`copy-schemas.ts` copies all `.json` from `schemas/v1/` without allowlist** — Any draft or test JSON dropped in `schemas/v1/` would be bundled and published. Currently 2 files; risk is low. Story 2.8 pre-publish cleanup can add an explicit allowlist if needed. [`caspian/packages/core/scripts/copy-schemas.ts:15-17`]
- **`exports` map missing explicit `"types"` conditions** — `"moduleResolution": "bundler"` TypeScript consumers may not resolve the `./diagnostics` sub-export types. Add `{ "types": ..., "default": ... }` condition objects before Story 2.8 npm publish. [`caspian/packages/core/package.json:13-16`]
- **`validateFile` accepts directory paths** — `fs.access()` succeeds on directories; stub returns `[]`. Production implementation (Story 2.3+ full file read) will naturally reject directories. [`caspian/packages/core/src/index.ts:6`]
- **`dist/.tsbuildinfo` published to npm with absolute machine paths** — `files: ["dist/"]` includes `.tsbuildinfo` which embeds the build machine's absolute source paths. Add `dist/.tsbuildinfo` to `.npmignore` or relocate `tsBuildInfoFile` outside `dist/` before Story 2.8 publish. [`caspian/packages/core/tsconfig.json:7`, `package.json:17`]

## Deferred from: code review of 2-2-diagnostic-registry-typed-ts-constants-codes-generated-ts-sha256-verify-hash (2026-04-27)

- **W1 — `replace("-", "_")` non-global:** all 18 current codes have exactly one hyphen so `String.replace` produces correct identifiers, but any future registry code with 2+ hyphens (e.g., `CASPIAN-E-EXTRA`) would emit an invalid JS identifier without a compile-time error (scripts excluded from tsconfig). Consider `replaceAll("-", "_")` as a cheap fix in a future story. [`caspian/packages/core/scripts/gen-diagnostic-codes.ts:51`]
- **W2 — sha256 computed over raw bytes (CRLF-sensitive):** generator and verifier hash the literal bytes from disk; if `registry.json` is ever checked out with CRLF line endings (Windows developer bypassing `.gitattributes`), the recomputed hash diverges from a Unix-generated baseline. `.gitattributes eol=lf` mitigates this but does not eliminate the risk (e.g., developers who cloned before the attribute was applied). Fix: normalize newlines before hashing.
- **W3 — Spurious `.git` cleanup silent skip:** `install-pre-commit.mjs` removes `caspian/.git/` only when `readdirSync` returns exactly `["hooks"]`. If `simple-git-hooks` ever writes additional artifacts there, the guard fails and the spurious directory persists silently. [`caspian/scripts/install-pre-commit.mjs:85`]
- **W4 — `absGitDir` base coincidentally equals execSync cwd:** the relative gitDir path is resolved using `path.resolve(here, "..", gitDir)` where `here/..` happens to be the same directory as the `execSync` cwd. If either is changed independently, the resolution silently points elsewhere. [`caspian/scripts/install-pre-commit.mjs:40`]
- **W5 — `RegistryEntry` interface duplicated:** the interface is defined independently in `gen-diagnostic-codes.ts` and `codes-shape.test.ts`; both are excluded from tsconfig, so no compile-time enforcement of alignment. A future registry schema change (e.g., adding a `deprecated` field) requires manual updates to both definitions.
- **W6 — Scripts excluded from tsconfig:** `gen-diagnostic-codes.ts`, `verify-codes-hash.ts`, `ajv-validate-registry.ts` are run by `tsx` only; type errors in these scripts are invisible to `tsc`. Accepted trade-off in this project pattern.
- **W7 — `prepare` installs hook in CI git workspaces:** the hook fires on `git commit` only (not on install), so most CI pipelines are unaffected. The `SKIP_SIMPLE_GIT_HOOKS=1` escape hatch exists in the hook header but is undocumented for CI operators in any CI config file.
- **W8 — CHANGELOG omits custom installer detail:** the deliberate AC2 departure (custom `install-pre-commit.mjs` instead of `simple-git-hooks || true`) is documented in sprint notes and the script header but not mentioned in `CHANGELOG.md`. Minor documentation gap.
- **README missing explicit "deliberate departure" label for async signature** — AC10 requires the README to document the `Promise<Diagnostic[]>` departure. The README shows the async signature but doesn't use the phrase "deliberate departure from epic AC wording". Reasoning is in Completion Notes. Low priority. [`caspian/packages/core/README.md:23`]

## Deferred from: code review of 2-3-pipeline-stages-1-3-caspian-core-byte-level-frontmatter-extraction-yaml-parse (2026-04-28)

- **`y`/`n` in `YAML_1_1_UNQUOTED_BOOLEANS` — false positive risk** — AC1 mandates inclusion of `"y"` and `"n"` as YAML 1.1 unquoted-boolean keywords flagged by E007. These single-character values are also commonly used as legitimate non-boolean strings in YAML configs. A plugin author using `user-invocable: y` as a shorthand would incorrectly receive E007. Spec-mandated per AC1; revisit as spec evolution if real-world false positives surface. [`caspian/packages/core/src/constants.ts`]
- **Unchecked cast `doc.toJS() as Record<string, unknown>`** — If a frontmatter YAML document root is a sequence (`- item`) or a bare scalar (`hello`), `doc.toJS()` returns an array or primitive, not a record. Stage 3 would return `{ data: [], diagnostics: [] }` and signal success. Stage 4 (Story 2.4) will validate envelope shape against the JSON schema, catching this case. [`caspian/packages/core/src/parsers/yaml.ts`]
- **Duplicated line-counting logic** — `countLinesUpTo` (`frontmatter.ts`) and `offsetToRawLine` (`yaml.ts`) implement the same character-by-character `\n`-counting algorithm in separate files with different names. Future divergence (e.g., adding CRLF-awareness to one but not the other) is a maintenance risk. Candidate for a shared `src/utils/lines.ts` helper in a future refactoring story. [`caspian/packages/core/src/parsers/frontmatter.ts`, `caspian/packages/core/src/parsers/yaml.ts`]
- **`---` inside a YAML block scalar misidentified as closing delimiter** — The regex `/\r?\n---(?:\r?\n|$)/g` in `findClosingDelim` matches any unindented `---` line, including one inside a block scalar value (`note: |\n  ---\n`). This truncates frontmatter prematurely and produces malformed YAML input to stage 3, which triggers E006. Fails safely (no silent pass), but the reported error is misleading. Inherent limitation of regex-based frontmatter extraction without a full YAML parser. [`caspian/packages/core/src/parsers/frontmatter.ts`]
